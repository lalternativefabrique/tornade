version: 1
kind: pipeline
name: ci

tools:
  node: "24.16.0"

on:
  branches: ['**', '!main']

scopes:
  app:
    - "src/**"
    - "package.json"
    - "package-lock.json"
    - "Dockerfile"

steps:
  - name: test
    scope: [app]
    tools: [node]
    run: |
      npm install
      npm test
